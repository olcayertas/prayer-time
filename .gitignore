# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj

# Xcode / build
.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/

# SwiftPM
.build/

# JetBrains IDEs (WebStorm / IntelliJ)
.idea/

# Claude Code — personal local overrides (CLAUDE.md, settings.json, commands are shared)
.claude/settings.local.json

# Serena — its own .serena/.gitignore excludes cache/ and project.local.yml;
# project.yml + memories/ are committed so onboarding is shared.
